# clean-architecture-dotnet-10

## Docker commands

- Run the containers in detached mode

```bash
docker-compose up -d
```

- Stop the containers
```bash
docker-compose stop
```

- Remove all existing containers
```bash
docker-compose down 
```
  